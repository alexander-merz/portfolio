import { TestBed } from '@angular/core/testing'

import { TechnologyColorService } from './technology-color.service'

describe('TechnologyColorService', () => {
  let service: TechnologyColorService

  beforeEach(() => {
    TestBed.configureTestingModule({})
    service = TestBed.inject(TechnologyColorService)
  })
})
